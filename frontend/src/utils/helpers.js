export const colors = {
  getIntensityColor: (intensity) => {
    if (intensity > 0.7) return '#FF1744';
    if (intensity > 0.4) return '#FF9800';
    return '#00C853';
  }
};

export const geo = {
  // Placeholder for geographic utilities
};
